原RAG检索流程：

    1、查询向量化：用户查询通过 DashScopeEmbeddings（阿里云通义千问嵌入模型）转换为高维向量。
    2、向量相似度搜索：在 Chroma 向量数据库中，使用 近似最近邻搜索 (ANN) 算法（默认 HNSW）查找与查询向量最相似的文档向量，相似度度量通常为 余弦相似度。
    3、Top‑K 召回：返回相似度最高的前 k 个文档块（k 在 config/chroma.yml 中配置）。
    4、上下文构建：将召回文档的 page_content 和 metadata 拼接成结构化上下文，与原始查询一起送入大模型生成最终回答。

代码执行路径：rag_summarize → retriever_docs → retriever.invoke(query) → vector_store.as_retriever() → Chroma 内置的相似度检索。


带Rerank的RAG检索流程：

    1、查询向量化：用户查询通过 DashScopeEmbeddings（阿里云通义千问嵌入模型）转换为高维向量。
    2、粗排检索（Coarse Retrieval）：在 Chroma 向量数据库中，使用近似最近邻搜索（ANN）算法查找与查询向量最相似的文档向量，召回较多候选文档（候选数在 config/rerank.yml 的 coarse_k 中配置，默认 10），作为粗排结果。
    3、Rerank精排（Fine-grained Ranking）：使用 Rerank 模型（BAAI/bge-reranker-base）对粗排候选文档进行重新排序。模型计算查询与每个候选文档的语义相关性得分，按得分降序排列。
    4、Top‑K 召回：取 Rerank 排序后的前 k 个文档块（k 在 config/rerank.yml 的 fine_k 中配置，默认 3）。
    5、上下文构建：将最终召回文档的 page_content 和 metadata 拼接成结构化上下文，与原始查询一起送入大模型生成最终回答。

代码执行路径：rag_summarize → retriever_docs → coarse_retriever.invoke(query) → reranker.rerank(query, coarse_docs, top_k=fine_k) → fine_docs → 上下文构建。

关键配置（config/rerank.yml）：
- coarse_k: 10     # 粗排候选数
- fine_k: 3        # 精排输出数
- rerank_model: "BAAI/bge-reranker-base"  # Rerank模型
- fallback_to_original: true  # 降级开关

降级机制：若 Rerank 服务加载失败或执行异常，且 fallback_to_original 为 true，系统自动降级到原始检索流程（粗排结果直接取 top-3）。