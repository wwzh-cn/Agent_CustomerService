原RAG检索流程：

    1、查询向量化：用户查询通过 DashScopeEmbeddings（阿里云通义千问嵌入模型）转换为高维向量。
    2、向量相似度搜索：在 Chroma 向量数据库中，使用 近似最近邻搜索 (ANN) 算法（默认 HNSW）查找与查询向量最相似的文档向量，相似度度量通常为 余弦相似度。
    3、Top‑K 召回：返回相似度最高的前 k 个文档块（k 在 config/chroma.yml 中配置）。
    4、上下文构建：将召回文档的 page_content 和 metadata 拼接成结构化上下文，与原始查询一起送入大模型生成最终回答。

代码执行路径：rag_summarize → retriever_docs → retriever.invoke(query) → vector_store.as_retriever() → Chroma 内置的相似度检索。


